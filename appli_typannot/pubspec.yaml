name: test_import_video
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.68.0  # Pour les graphiques
    
  path: ^1.9.0
  path_provider: ^2.1.2
  chewie: ^1.5.0

  video_player: ^2.10.0
  file_picker: ^10.0.0
  ffmpeg_kit_flutter_new: ^4.1.0

  image: ^4.1.3
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
  assets:
      - lib/config.json
